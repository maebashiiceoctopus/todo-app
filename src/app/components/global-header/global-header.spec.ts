/* import { render } from '@stencil/core/testing';
import { GlobalHeader } from './global-header';

describe('global-header', () => {
  it('should build', () => {
    expect(new GlobalHeader()).toBeTruthy();
  });

  describe('rendering', () => {
    beforeEach(async () => {
      await render({
        components: [GlobalHeader],
        html: '<global-header></global-header>'
      });
    });
  });
}); */